server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql:///jmc?serverTimezone=UTC
    username: root
    password: root

# 为mapper开启debug模式（输出sql语句）
logging:
  level:
    com.jmc.mapper:
      debug

