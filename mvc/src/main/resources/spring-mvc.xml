<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="com.jmc.controller"/>

    <!-- 配置注解驱动 -->
    <mvc:annotation-driven/>
    <!-- 放开对非mvc资源的限制，使用初始servlet配置 -->
    <mvc:default-servlet-handler/>

    <!-- 拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/func"/>
            <bean class="com.jmc.config.interceptor.FuncInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 简单异常映射器 -->
    <bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
        <!--<property name="defaultErrorView" value=""/>-->
        <property name="exceptionMappings">
            <map>
                <entry key="java.lang.RuntimeException" value="/error"/>
            </map>
        </property>
    </bean>
</beans>